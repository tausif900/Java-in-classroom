package collections;

public class Example8 {
	public static void main(String[] args) {

	}
}

//	Set - It has only unique values.It has three implementations HashSet,LinkedHashSet and TreeSet.All three has same features
// HashSet doesn't has guarantee sequence.Hashing technique is used internally.
//LinkedHashSet internally inherits HashSet only, but the structure that they are using is LinkedList. Data is ordered in LinkedHashSet.
//TreeSet - Values are unique. Things are sorted already.Integers sorted in ascending.Product,student then we have to decide.